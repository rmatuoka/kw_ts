-content_for :head do
  = stylesheet_link_tag "perfil.css"
  
.container-fluid.top_perfil
  .container
    .row
      .col-md-12.perfil
        .col-md-3.avatar-perfil
          -if current_user.picture.file?
            %a{:href => "#"}
              =image_tag(current_user.picture.url(:medium))
          -else
            =image_tag("user_missing_medium.png")

        .col-md-9
          .row
            %h2="#{current_user.name} #{current_user.last_name}"
            - if current_user.services_performed.present?
              %p="#{current_user.services_performed}" 
          .row
            %a{:href => "#{edit_user_path(current_user)}"}
              =image_tag("btn-editar-perfil.png")
.container-fluid.dados_perfil
  .container
    .row
      .col-md-6
        .dados_perfil_esquerda
          .row
            %h1 Ajude-nos a melhorar
            .caixa_ideias
              .row
                %p Ajude-nos a melhorar nosso site.Que Bom... – Deixe o seu elogio. Que Pena...  – Deixe suas críticas e fale daquilo que não gostou. Que Tal... – Deixe suas ideias, sugestões, fale de algo que você não encontrou aqui. Utilize esta área para indicar uma empresa de treinamento que não tenha encontrado no TS.
              .row
                .col-md-2
                  -if current_user.picture.file?
                    %a{:href => "#"}
                      =image_tag(current_user.picture.url(:small))
                  -else
                    =image_tag("user_missing_small.png")
                .col-md-10
                  .alert.alert-success.alert-dismissable
                    Sua mensagem foi enviada com sucesso!
                    %br
                    Obrigado por compartilhar sua opinição conosco.
      .col-md-5.col-md-offset-1
        .dados_perfil_direita
          =render file: "layouts/widgets/_empresas.html.haml"

